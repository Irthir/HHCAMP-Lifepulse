[project]
name = "backend"
version = "0.1.0"
description = "LifePulse backend API (FastAPI + event detection)"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic"
]